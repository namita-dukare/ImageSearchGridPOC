<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cavista.testapp">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_search_black_24dp"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_search_black_24dp"
        android:supportsRtl="true"
        android:name=".ApplicationClass"
        android:theme="@style/AppTheme">
        <activity android:name=".imagelist.view.ImageListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".imagedetails.view.ImageDetailActivity"
            android:windowSoftInputMode="adjustPan"
            >
            <!--<intent-filter>
                &lt;!&ndash;<action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />&ndash;&gt;
            </intent-filter>-->
        </activity>
    </application>

</manifest>